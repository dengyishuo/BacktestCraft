% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/add_quantile_signal.R
\name{add_quantile_signal}
\alias{add_quantile_signal}
\title{Generate signal by selecting top/bottom quantile of stocks each day}
\usage{
add_quantile_signal(
  df,
  rank_col,
  quantile = 0.2,
  select = c("top", "bottom"),
  signal_name = NULL,
  output_type = c("tibble", "data.frame")
)
}
\arguments{
\item{df}{Data frame in long format, must contain 'date' and 'code' columns}

\item{rank_col}{Character string of column name to rank (e.g., "mom_20")}

\item{quantile}{Numeric between 0 and 1. For top selection, the top `quantile`
fraction of stocks (e.g., 0.2 = top 20\%). For bottom selection, use `select = "bottom"`.}

\item{select}{Character: "top" (higher values are better) or "bottom"}

\item{signal_name}{Output signal column name. Auto-generated if NULL.}

\item{output_type}{Output format: "tibble" (default) or "data.frame"}
}
\value{
Data frame with an appended 0/1 signal column (1 = selected by quantile)
}
\description{
Within each trading day, rank the indicator and select stocks that fall
above a given quantile (e.g., top 20%) or below a quantile (bottom 20%).
This is a flexible alternative to \code{add_rank_signal} when you want
to select a variable number of stocks (percentage-based) rather than a fixed count.
}
\examples{
\dontrun{
# Select top 20\% stocks by momentum each day
df <- add_quantile_signal(df, rank_col = "mom_20", quantile = 0.2, select = "top")

# Select bottom 10\% by volatility
df <- add_quantile_signal(df, rank_col = "vol_60", quantile = 0.1, select = "bottom")
}
}
